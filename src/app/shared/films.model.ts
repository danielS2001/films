export interface film {
    name: string;
    duration: number;
    progress: number;
    watched: boolean;
    percent: number;
}

export interface newFilm {
    name: string;
    duration: string;
}